import {StyleSheet, Dimensions} from 'react-native';

const styles = StyleSheet.create({
  image: {
    width: 43,
    position: 'relative',
    height: 43,
    alignSelf: 'center',
    tintColor: '#DBD22A',
  },
});

export default styles;
